FROM node:alpine

RUN mkdir /var/test
WORKDIR /var/test
COPY package.json /var/test/
RUN npm install && npm install -g mocha
COPY src/ /var/test/
CMD ["echo", "$(pwd)"]
CMD ["echo", "$(ls)"]

CMD ["mocha", "/var/test/src/tests", "--recursive" , "--reporter", "spec"]
